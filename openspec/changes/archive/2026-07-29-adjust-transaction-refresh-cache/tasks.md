## 1. Refresh flow

- [x] 1.1 Update the refresh logic so it fetches data before writing to cache.
- [x] 1.2 Preserve the existing cache when the refresh request fails.
- [x] 1.3 Replace the cache and update the visible list only after a successful response.

## 2. Validation

- [x] 2.1 Verify the app keeps the current cached data when refresh fails.
- [x] 2.2 Verify the app updates the cache and list when refresh succeeds.
